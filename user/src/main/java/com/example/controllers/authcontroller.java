package com.example.controllers;

//import org.springframework.beans.factory.annotation.Autowired;

public class authcontroller {
/*
	@Autowired
    EmployeService EmployeService;

    @Override
    public Authentication authenticate(Authentication authentication) throws AuthenticationException {
//        return super.authenticate(authentication);
        UsernamePasswordAuthenticationToken auth = (UsernamePasswordAuthenticationToken) authentication;
        String name = auth.getName();
        String password = auth.getCredentials().toString();

        UserDetails centre = EmployeService.loadUserByUsername(name);

        if(centre == null){
            throw new BadCredentialsException("User/Password est incorect"+Employe.getPrincipal());
        }

//        return new UsernamePasswordAuthenticationToken(centre , null, centre.getAuthorities());
        return new UsernamePasswordAuthenticationToken(Employe,null, Employe.getAuthorities());
    }

    @Override
    public boolean supports(Class<?> authentication) {
//        return super.supports(authentication);
        return true;
    }
*/
}
